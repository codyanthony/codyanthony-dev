# build output
dist/
# generated types
.astro/
# wrangler / miniflare local dev state (local R2/KV/cache, deploy temp)
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# Local tooling — not for public consumption
# (CLAUDE.md is tracked: it's shared project guidance, version-controlled with the code)
.claude/
scripts/
.markdownlint-cli2.jsonc

# Photo source set (189MB) — used to generate site headshot and LinkedIn assets
Cody Pro Photos 2026/
